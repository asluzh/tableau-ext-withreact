import React from 'react';
import ReactDOM from 'react-dom/client';
import Configure from './components/Configure/Configure.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Configure />
  </React.StrictMode>
);
